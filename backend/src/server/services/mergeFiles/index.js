/**
 * Function to merge the two string in one, in future can be modified due to the specification
 * @param file1
 * @param file2
 */
function mergeRequests(file1, file2) {
    return file1 + ' ' + file2;
}

export {
    mergeRequests
}