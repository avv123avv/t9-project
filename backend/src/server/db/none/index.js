const connect       = () => {};
const passport      = () => {};
const session       = () => {};

import controllers from './controllers';

export default {
    connect,
    controllers,
    passport,
    session
};